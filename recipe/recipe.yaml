schema_version: 1

context:
  name: proxystore
  version: "1.1.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/proxystore-${{ version }}.tar.gz
  sha256: b799a535b8a939b3ebab3eed19d529f8a411ded1c9b87f3d1df0be864856c1e2

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - proxystore-endpoint = proxystore.endpoint.cli:cli
      - proxystore-globus-auth = proxystore.globus.cli:cli
      - proxystore-relay = proxystore.p2p.relay.run:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77.0
    - setuptools-scm
    - pip
  run:
    - python >=${{ python_min }}
    - click !=8.1.4
    - cloudpickle >=3
    - cryptography >=39.0.1
    - globus-sdk >=4,<5
    - pydantic >=2,<3
    # for all python, not just py<3.11, to stay noarch: python
    - tomli
    - tomli-w
    - typing_extensions >=4.3.0

tests:
  - python:
      imports:
        - proxystore
      pip_check: true
      python_version: ${{ python_min }}
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pip
        - websockets
        - tosholi
        - pydantic
    script:
      - proxystore-globus-auth --help
      - proxystore-relay --help

about:
  summary: Lazy object proxy interface for distributed stores.
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/gpauloski/ProxyStore

extra:
  recipe-maintainers:
    - WardLT
    - jan-janssen
    - xylar
